package lapr.project.model;

public class Admin extends User {
    /**
     * Constructor responsible to create an Admin
     * @param idUser
     * @param password 
     */
    public Admin(String idUser, String password) {
        super(idUser, password);
    }
}
